<!DOCTYPE html>
<html lang="en">

<head>
  <% include ../partials/head.ejs %>
  <title>SH2020 Hub - Discord Authentication</title>
</head>

<body>
  <div class="wrapper">
    <% include ../partials/navbar.ejs %>

    <div class="main-panel">
      <div class="content">
        <div class="container">
          <% if (!error) { %>
          <h3>Success! Your account is now linked!</h3>
          <p><a href="<%= discordUrl %>" target="_blank">Open Discord</a> to access the Hackathon server!</p>
          <% } else { %>
          <h3>An error occurred.</h3>
          <p><%= error %></p>
          <p>Try again in a few seconds.</p>
          <% } %>
        </div>
      </div>
      <% include ../partials/footer.ejs %>
    </div>
  </div>
</body>

</html>