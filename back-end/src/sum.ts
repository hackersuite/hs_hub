/**
 * A function to showcase unit testing with Jest
 * the test implementation can be found at ../test/unit/sum.test.ts
 */

 export const sum = (a: number, b: number): number => {
   return a + b;
 };